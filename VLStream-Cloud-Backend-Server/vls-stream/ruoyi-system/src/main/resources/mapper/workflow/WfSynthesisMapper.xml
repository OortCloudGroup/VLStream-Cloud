<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.workflow.mapper.WfSynthesisMapper">
    <resultMap type="com.ruoyi.workflow.domain.WfSynthesis" id="WfSynthesisResult">
        <result property="synthesisId" column="synthesis_id"/>
        <result property="parentId" column="parent_id"/>
        <result property="categoryName" column="category_name"/>
        <result property="tenantId" column="tenant_id"/>
        <result property="userId" column="user_id"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="delFlag" column="del_flag"/>
    </resultMap>
    <select id="selectChildById" resultType="java.lang.String">
        WITH RECURSIVE synthesis_tree AS (
        SELECT synthesis_id ,tenant_id,del_flag
        FROM wf_synthesis
        <if test="parentId != null and parentId != ''">
            WHERE synthesis_id = #{parentId} -- 父节点ID
        </if>
        UNION ALL
        SELECT ws.synthesis_id,ws.tenant_id,ws.del_flag
        FROM wf_synthesis ws
        INNER JOIN synthesis_tree st ON ws.parent_id = st.synthesis_id
        )
        SELECT synthesis_id FROM synthesis_tree
        <where>
            and del_flag='0'
        </where>;
    </select>
</mapper>
