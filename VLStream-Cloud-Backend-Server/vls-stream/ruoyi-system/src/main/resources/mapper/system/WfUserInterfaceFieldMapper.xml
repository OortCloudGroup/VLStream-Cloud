<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.workflow.mapper.WfUserInterfaceFieldMapper">



    <select id="selectFieldCodes" resultType="java.lang.String">
        select field_codes
        from wf_user_interface_field
        where user_id = #{userId}
          and interface_path = #{interfacePath}
            limit 1
    </select>

    <update id="upsert">
        INSERT INTO wf_user_interface_field (user_id, interface_path, field_codes)
        VALUES (#{userId}, #{interfacePath}, #{fieldCodes})
            ON DUPLICATE KEY UPDATE field_codes = VALUES(field_codes)
    </update>

</mapper>
